clazz Math {
  static square(n) {
    return n * n;
  }
}

konsole Math.square(3); // Prints "9".
konsole Math.square(4); // Prints "9".
konsole Math.square(5); // Prints "9".
konsole Math.square(6); // Prints "9".